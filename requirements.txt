# Jev vs Qwen AMR comparison pipeline — Python dependencies
# Pipeline scripts (predict_amr_catalog.py, predict_amr_llm.py, compare_models.py, amr_common.py)
requests>=2.31.0

# Walkthrough document generator (make_walkthrough_docx.py)
python-docx>=1.1.0
